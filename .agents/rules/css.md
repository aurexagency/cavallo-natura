---
trigger: always_on
---

CSS.MD
---
trigger: always_on
---

* Utilizza le CSS variables (definite nel file `globals.css` e mappate nel `tailwind.config.ts`) per i colori principali, ispirati alla natura: toni caldi della terra (marrone sella, beige sabbia), verde pino e tocchi eleganti per i contrasti.
* Evita di usare stili inline.